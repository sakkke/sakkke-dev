import MdxLayout from "../components/mdx-layout";

export const metadata = {
  title: "sakkke's social links",
  description: "sakkke's social links.",
  created: "2024-04-03",
  updated: "2024-04-05",
};

- **GitHub**: [sakkke](https://github.com/sakkke)
- **Unix & Linux Stack Exchange**: [sakkke](https://unix.stackexchange.com/users/514537/sakkke)
- **Zenn**: [sakkke](https://zenn.dev/sakkke)

export default function MDXPage({ children }) {
  return (
    <MdxLayout
      created={metadata.created}
      title={metadata.title}
      updated={metadata.updated}
    >
      {children}
    </MdxLayout>
  );
}
