import MdxLayout from "../components/mdx-layout";

export const metadata = {
  title: "sakkke's tools",
  description: "sakkke's tools.",
  created: "2024-04-03",
  updated: "2024-04-29",
};

- **[sakkke wc](/tools/wc)**: 'sakkke wc' is word count tool. it counts lines, words, and bytes.
- **[sakkke sort](/tools/sort)**: 'sakkke sort' is sort tool. it sorts lines of text.
- **[sakkke grep](/tools/grep)**: 'sakkke grep' is search tool. it searchs for pattern in text.
- **[noblanks](/tools/noblanks)**: 'noblanks' is a tool to remove blank lines automatically.
- **[replace](/tools/replace)**: 'replace' is replace tool. it substitutes pattern with replacement.

export default function MDXPage({ children }) {
  return (
    <MdxLayout
      created={metadata.created}
      title={metadata.title}
      updated={metadata.updated}
    >
      {children}
    </MdxLayout>
  );
}
