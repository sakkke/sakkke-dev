import MdxLayout from "../components/mdx-layout";

export const metadata = {
  title: "sakkke's community",
  description: "sakkke's community.",
  created: "2024-04-05",
  updated: "2024-04-05",
};

- **Discord community**: [sakkke.dev](https://discord.gg/53gZbpvKjg)

export default function MDXPage({ children }) {
  return (
    <MdxLayout
      created={metadata.created}
      title={metadata.title}
      updated={metadata.updated}
    >
      {children}
    </MdxLayout>
  );
}
